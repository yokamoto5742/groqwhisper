[tool.pytest.ini_options]
# カバレッジレポートを作成するときはコメントアウトを解除
addopts = [
#    "--cov",
#    "--cov-report=html",
#    "--cov-report=term-missing",
#    "--cov-config=.coveragerc",
#    "-v"
]

testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]